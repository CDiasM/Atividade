#include <stdio.h>

int main() {
    int n, i, soma = 0;

    printf("Digite um número: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i += 2) {
        soma += i;
    }

    printf("Soma dos números ímpares de 1 até %d: %d\n", n, soma);
    return 0;
}
