#include <stdio.h>

int main() {
    int n, i, soma = 0;

    printf("Digite um número: ");
    scanf("%d", &n);

    for (i = 2; i <= n; i += 2) {
        soma += i;
    }

    printf("Soma dos números pares de 2 até %d: %d\n", n, soma);
    return 0;
}
