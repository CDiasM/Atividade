#include <stdio.h>

int main() {
    int a, b;

    printf("Digite dois números: ");
    scanf("%d %d", &a, &b);

    if (a == b) {
        printf("Valores iguais\n");
    } else if (a < b) {
        printf("Menor: %d\n", a);
    } else {
        printf("Menor: %d\n", b);
    }

    return 0;
}
